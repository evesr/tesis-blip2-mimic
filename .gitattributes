# Git LFS DESHABILITADO: git-lfs no está instalado y los pesos de modelos
# (*.safetensors, *.pt, *.pth, *.bin), comprimidos (*.gz, *.zip) y datasets
# grandes se excluyen vía .gitignore (superan el límite de 100 MB de GitHub).
# No se versiona ningún binario grande en este repositorio.
