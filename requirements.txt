# Requirements para proyecto BLIP2 + LoRA
# Tesis - Generación de reportes médicos a partir de radiografías
# Python 3.8+

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0

# Hugging Face ecosystem — versiones pineadas (probadas mayo 2026)
# NOTA: peft>=0.10 rompe get_peft_model sobre Blip2ForConditionalGeneration completo
# porque inyecta inputs_embeds que el forward de BLIP-2 no acepta.
# Solución adoptada: LoRA se aplica a base_model.language_model (OPT interno).
transformers==4.46.3
accelerate>=0.20.0
peft==0.13.2
datasets>=2.12.0

# Quantization
bitsandbytes>=0.39.0

# Data processing
pandas>=1.5.0
numpy>=1.23.0
pillow>=9.5.0
opencv-python>=4.7.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# NLP Metrics (para evaluación)
nltk>=3.8.0
rouge-score>=0.1.2

# Medical NLP (opcional - para RadGraph)
radgraph>=0.1.0
scispacy>=0.5.1

# Optional: for advanced logging and tracking
# tensorboard>=2.13.0
# wandb>=0.15.0

# Development and testing
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0
mypy>=1.3.0
